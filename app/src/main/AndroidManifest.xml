<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DDIP_Client"
        tools:targetApi="31">
        <activity
            android:name=".StaffMainActivity"
            android:exported="false" />
        <activity
            android:name=".OwnerMainActivity"
            android:exported="false" /> <!-- LoginSignupActivity를 첫 화면으로 설정 -->
        <activity
            android:name=".LoginSignupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- SignupActivity 등록 -->
        <activity
            android:name=".SignupActivity"
            android:exported="true" /> <!-- MainActivity 등록 -->
        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- CrewRoomActivity 등록 -->
        <activity
            android:name=".CrewRoomActivity"
            android:exported="true" />
        <activity
            android:name=".CalendarActivity"
            android:exported="true" />
        <activity
            android:name=".AddWorkActivity"
            android:exported="true" />
        <activity android:name=".AlarmActivity" />
        <activity android:name=".MypageActivity" />
        <activity android:name=".UserListActivity" />
    </application>

</manifest>