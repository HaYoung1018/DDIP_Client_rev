<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DDIP_Client"
        tools:targetApi="31">

        <!-- 첫 화면 설정 -->
        <activity
            android:name=".LoginSignupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 각 Activity 등록 -->
        <activity android:name=".SignupActivity" android:exported="true" />
        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".CrewRoomActivity" android:exported="true" />
        <activity android:name=".CrewRoomListActivity" android:exported="true" />
        <activity android:name=".CalendarActivity" android:exported="true" />
        <activity android:name=".AddWorkActivity" android:exported="true" />
        <activity android:name=".InviteCodeActivity" android:exported="true" />
        <activity android:name=".AlarmActivity" android:exported="true" />
        <activity android:name=".MypageActivity" android:exported="true" />
        <activity android:name=".OwnerMainActivity" android:exported="true" />
        <activity android:name=".UserListActivity" android:exported="true" />
        <activity android:name=".StaffMainActivity" android:exported="true" />

    </application>

</manifest>
